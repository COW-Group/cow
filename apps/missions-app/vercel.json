{
  "version": 2,
  "framework": null,
  "installCommand": "echo 'Skipping default install'",
  "buildCommand": "sleep 2 && cp .npmrc ../.. && cd ../.. && npm ci --legacy-peer-deps && cd apps/missions-app && npm run build",
  "outputDirectory": "dist",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
